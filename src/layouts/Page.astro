---
import '../styles/theme.css';
import '../styles/components.css';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { getLocalizedHref, type Language } from '../utils/i18n';

interface NavItem {
  href: string;
  label: string;
  children?: NavItem[];
}

interface Props {
  title: string;
  description?: string;
  content?: any;
  section?: 'about' | 'environment' | 'community' | 'history';
}

const { title, description, content, section } = Astro.props;
const { Content } = content || {};

const currentPath = Astro.url.pathname;
const currentLang = (Astro.currentLocale || 'en') as Language;

// Define navigation items for each section
const aboutNav: { en: NavItem[]; fr: NavItem[] } = {
  en: [
    { href: '/about', label: 'About the Association' },
    { href: '/about/executive', label: 'Association Executive' },
    { href: '/about/bylaws', label: 'Association Bylaws' },
    { href: '/about/business-records', label: 'Recent Business Records' },
    { href: '/about/archive', label: 'Archive Portal' },
  ],
  fr: [
    { href: '/fr/a-propos', label: 'À propos de l\'Association' },
    { href: '/fr/a-propos/comite-executif', label: 'Comité exécutif' },
    { href: '/fr/a-propos/reglements', label: 'Règlements de l\'Association' },
    { href: '/fr/a-propos/proces-verbaux', label: 'Procès-verbaux récents' },
    { href: '/fr/a-propos/archives', label: 'Portail d\'archives' },
  ],
};

const environmentNav: { en: NavItem[]; fr: NavItem[] } = {
  en: [
    { href: '/environment', label: 'Environment' },
    { href: '/environment/water-sampling', label: 'Water Sampling Program' },
    { href: '/environment/milfoil', label: 'Eurasian Milfoil' },
    { href: '/environment/shoreline', label: 'Protecting Our Shoreline' },
    { href: '/environment/water', label: 'Protecting Our Water' },
    { href: '/environment/boating', label: 'Boating on Lac Bernard' },
    { href: '/environment/wildlife', label: 'Lake Wildlife' },
  ],
  fr: [
    { href: '/fr/environnement', label: 'Environnement' },
    { href: '/fr/environnement/echantillonnage-eau', label: 'Programme d\'échantillonnage de l\'eau' },
    { href: '/fr/environnement/myriophylle', label: 'Myriophylle à épi' },
    { href: '/fr/environnement/rivage', label: 'Protéger notre rivage' },
    { href: '/fr/environnement/eau', label: 'Protéger notre eau' },
    { href: '/fr/environnement/navigation', label: 'Navigation sur le lac Bernard' },
    { href: '/fr/environnement/faune', label: 'Faune du lac' },
  ],
};

const communityNav: { en: NavItem[]; fr: NavItem[] } = {
  en: [
    { href: '/community', label: 'Community' },
    { href: '/community/regatta', label: 'Annual Regatta' },
    { 
      href: '/history', 
      label: 'History',
      children: [
        { href: '/history/first-nations', label: 'First Nations' },
        { href: '/history/fishing-club', label: 'Fishing Club' },
        { href: '/history/maps', label: 'Maps' },
        { href: '/history/ariel-photos', label: 'Ariel Photos' },
        { href: '/history/stories-documenting-families-and-individuals', label: 'Stories: Documenting Families and Individuals' },
        { href: '/history/stories-documenting-history-of-the-lake', label: 'Stories: Documenting History of the Lake' },
        { href: '/history/newspaper-clippings-collection-of-lac-bernard', label: 'Newspaper Clippings Collection of Lac Bernard' },
        { href: '/history/historical-research-and-sources', label: 'Historical Research and Sources' },
        { href: '/history/properties-and-deeds', label: 'Properties and Deeds' },
        { href: '/history/related-history', label: 'Related History' },
      ]
    },
    { 
      href: '/community/security', 
      label: 'Security',
      children: [
        { href: '/community/security/police', label: 'Police' },
        { href: '/community/security/emergency-contacts', label: 'Emergency Contacts' },
        { href: '/community/security/fire', label: 'Fire' },
        { href: '/community/security/neighbourhood-watch', label: 'Neighbourhood Watch Program' },
      ]
    },
  ],
  fr: [
    { href: '/fr/communaute', label: 'Communauté' },
    { href: '/fr/communaute/regate', label: 'Régate annuelle' },
    { 
      href: '/fr/histoire', 
      label: 'Histoire',
      children: [
        { href: '/fr/histoire/premieres-nations', label: 'Premières Nations' },
        { href: '/fr/histoire/club-de-peche', label: 'Club de pêche' },
        { href: '/fr/histoire/cartes', label: 'Cartes' },
        { href: '/fr/histoire/photos-aeriennes', label: 'Photos aériennes' },
        { href: '/fr/histoire/recits-familles-et-individus', label: 'Récits: Documentation des familles et individus' },
        { href: '/fr/histoire/recits-histoire-du-lac', label: 'Récits: Documentation de l\'histoire du lac' },
        { href: '/fr/histoire/collection-coupures-de-journaux-lac-bernard', label: 'Collection de coupures de journaux du lac Bernard' },
        { href: '/fr/histoire/recherche-historique-et-sources', label: 'Recherche historique et sources' },
        { href: '/fr/histoire/proprietes-et-titres', label: 'Propriétés et titres' },
        { href: '/fr/histoire/histoire-connexe', label: 'Histoire connexe' },
      ]
    },
    { 
      href: '/fr/communaute/securite', 
      label: 'Sécurité',
      children: [
        { href: '/fr/communaute/securite/police', label: 'Police' },
        { href: '/fr/communaute/securite/contacts-urgence', label: 'Contacts d\'urgence' },
        { href: '/fr/communaute/securite/incendie', label: 'Incendie' },
        { href: '/fr/communaute/securite/surveillance-quartier', label: 'Programme de surveillance de quartier' },
      ]
    },
  ],
};

// Use community nav for both community and history sections
const navItems = section === 'about' ? aboutNav[currentLang] : section === 'environment' ? environmentNav[currentLang] : (section === 'community' || section === 'history') ? communityNav[currentLang] : null;
---

<html lang={currentLang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} | Lac Bernard Association</title>
    <meta
      name="description"
      content={description || `Learn more about ${title} at the Lac Bernard Association.`}
    />
  </head>

  <body>
    <SiteHeader />

    <main id="main-content">
      <section class="section">
        <div class="container">
          {navItems ? (
            <div class="pageWithSidebar">
              <aside class="sidebar">
                <nav class="sidebarNav" aria-label="Section navigation">
                  {navItems?.map((item) => {
                    const normalizedHref = item.href.replace(/^\/fr/, '');
                    const isActive = currentPath === item.href || currentPath === normalizedHref || currentPath === getLocalizedHref(normalizedHref, currentLang);
                    const hasChildren = item.children && item.children.length > 0;
                    // Always show children if they exist
                    const showChildren = hasChildren;
                    
                    return (
                      <div class="navItem">
                        <a 
                          href={getLocalizedHref(normalizedHref, currentLang)} 
                          class={isActive ? 'active' : ''}
                        >
                          {item.label}
                        </a>
                        {showChildren && item.children && (
                          <div class="navChildren">
                            {item.children.map((child) => {
                              const childNormalizedHref = child.href.replace(/^\/fr/, '');
                              const isChildActive = currentPath === child.href || currentPath === childNormalizedHref || currentPath === getLocalizedHref(childNormalizedHref, currentLang);
                              return (
                                <a 
                                  href={getLocalizedHref(childNormalizedHref, currentLang)} 
                                  class={isChildActive ? 'active' : ''}
                                >
                                  {child.label}
                                </a>
                              );
                            })}
                          </div>
                        )}
                      </div>
                    );
                  })}
                </nav>
              </aside>
              <div class="pageContent prose">
                <h1>{title}</h1>
                {Content && <Content />}
              </div>
            </div>
          ) : (
            <div class="prose">
              <h1>{title}</h1>
              {Content && <Content />}
            </div>
          )}
        </div>
      </section>
    </main>

    <SiteFooter />
  </body>
</html>
