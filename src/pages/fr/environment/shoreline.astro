---
import Page from '../../../layouts/Page.astro';

const [page] = await Astro.glob('../../../../content/pages/fr/environment-shoreline.md');
if (!page) throw new Error('Missing content/pages/en/environment-shoreline.md');

const title = page.frontmatter?.title || 'Protecting Our Shoreline';
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} section="environment" />
