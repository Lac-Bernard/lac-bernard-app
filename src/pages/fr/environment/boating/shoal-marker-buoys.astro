---
import Page from '@/layouts/Page.astro';
import { loadContentPage } from '@/utils/loadContentPage';

const page = await loadContentPage('fr', 'environment/boating/shoal-marker-buoys');
const title = page.frontmatter?.title ?? "Bouées marqueurs de hauts-fonds";
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} section="environment" />
