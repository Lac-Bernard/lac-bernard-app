---
import Page from '../../layouts/Page.astro';

const [page] = await Astro.glob('../../../content/pages/community-security.md');
if (!page) throw new Error('Missing content/pages/community-security.md');

const title = page.frontmatter?.title || 'Security';
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} section="community" />
