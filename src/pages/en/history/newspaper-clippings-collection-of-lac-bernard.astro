---
import Page from '../../../layouts/Page.astro';
import { type Language } from '../../../utils/i18n';

const currentPath = Astro.url.pathname;
const currentLang = (Astro.currentLocale || 'en') as Language;

const [page] = currentLang === 'fr' 
  ? await Astro.glob('../../../../content/pages/fr/history-newspaper-clippings.md')
  : await Astro.glob('../../../../content/pages/en/history-newspaper-clippings.md');
if (!page) throw new Error(`Missing content/pages/${currentLang === 'fr' ? 'fr/' : 'en/'}history-newspaper-clippings.md`);

const titles = {
  en: 'Newspaper Clippings Collection of Lac Bernard',
  fr: 'Collection de coupures de journaux du lac Bernard',
};

const title = page.frontmatter?.title || titles[currentLang];
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} section="history" />
