---
import Page from '../../../../layouts/Page.astro';
import { type Language } from '../../../../utils/i18n';

const currentPath = Astro.url.pathname;
const currentLang = (Astro.currentLocale || 'en') as Language;

const [page] = currentLang === 'fr' 
  ? await Astro.glob('../../../../../content/pages/fr/environment-water-sampling-2023.md')
  : await Astro.glob('../../../../../content/pages/en/environment-water-sampling-2023.md');
if (!page) throw new Error(`Missing content/pages/${currentLang === 'fr' ? 'fr/' : 'en/'}environment-water-sampling-2023.md`);

const titles = {
  en: 'Report on Water Quality Sampling 2023',
  fr: 'Rapport sur l\'échantillonnage de la qualité de l\'eau 2023',
};

const title = page.frontmatter?.title || titles[currentLang];
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} section="environment" />
