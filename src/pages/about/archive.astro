---
import Page from '../../layouts/Page.astro';

const [page] = await Astro.glob('../../../content/pages/about-archive.md');
if (!page) throw new Error('Missing content/pages/about-archive.md');

const title = page.frontmatter?.title || 'Archive Portal';
const description = page.frontmatter?.description;
---

<Page title={title} description={description} content={page} />
