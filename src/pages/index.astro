---
import { homePathFromLocaleCookie, LOCALE_PREFERENCE_COOKIE } from '../utils/i18n';

return Astro.redirect(
	homePathFromLocaleCookie(Astro.cookies.get(LOCALE_PREFERENCE_COOKIE)?.value),
	302,
);
---
