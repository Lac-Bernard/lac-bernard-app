TINA_CLIENT_ID=
TINA_TOKEN=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
# Canonical site URL for magic-link / OAuth redirects. Set in Vercel for Production (and Preview if needed).
# Resolved at runtime on the server (process.env). Redeploy after changing. Alias: SITE_URL (server-only).
PUBLIC_SITE_URL=
SITE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=